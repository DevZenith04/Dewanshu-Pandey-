# Sidebar reference verification

The local dashboard loaded with the supplied reference direction applied: a near-black field-intelligence sidebar, coordinate header, `Zameen Vivaad` wordmark, vertical dashed navigation spine with orange active node, `Field signal` meter, synced registry status, and demo-account footer.

The desktop collapse control changed the sidebar to a narrow persistent rail and kept the main dashboard visible. The control exposed `aria-expanded`, changed its accessible label from `Collapse sidebar` to `Expand sidebar`, and persisted the collapsed state in local storage. The existing navigation and charts remained rendered.

The compact rail was refined to retain recognizable Material Symbols navigation icons instead of empty nodes. Re-expanding restored the complete reference sidebar, including the coordinate header, timeline labels, signal block, account card, and collapse control. The main dashboard remained intact throughout.
