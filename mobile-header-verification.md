# Mobile sidebar header verification

The provided screenshot was used as the visual target for the sidebar header. The repair keeps the brand icon, `Zameen` wordmark, `Vivaad / intelligence` descriptor, and close control within one deliberate header row instead of allowing the text block to float or wrap unpredictably.

A true narrow browser context was opened at a 500 px viewport with the mobile navigation expanded. The computed layout reported a 320 px sidebar, an 86 px brand row, a 48 px brand mark, a 155 px brand-copy column, 35 px `Barlow Condensed` wordmark text, 16 px `Newsreader` descriptor text, and a 48 px close button. The brand row has a subtle bottom divider and 20 px bottom padding, preventing the large blank gap seen in the supplied image.

The rendered screenshot shows the wordmark on one line, the descriptor on one line beneath it, a larger balanced icon, an aligned close button, a clear header divider, and compact navigation spacing. The background dashboard remains visible outside the sidebar, while the sidebar itself reads as a distinct glass surface.
